<?xml version="1.0" encoding="utf-8"?>

<!--
______________________________Image Gallery Activity - Lab 7 - Part 3______________________________
-->

<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <GridView
        android:id="@+id/grid_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:columnWidth="@dimen/column_width_gallery"
        android:numColumns="auto_fit"
        android:verticalSpacing="@dimen/vert_spacing_gallery"
        android:horizontalSpacing="@dimen/hor_spacing_gallery"
        android:layout_margin="@dimen/layout_marg_gallery"
        android:stretchMode="columnWidth"
        android:gravity="center"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/add_image_fab"
        android:background="@color/blue_500"
        android:backgroundTint="@color/blue_600"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:clickable="true"
        app:fabSize="normal"
        app:srcCompat="@drawable/ic_add_black_24dp"
        tools:ignore="VectorDrawableCompat" />

</android.support.design.widget.CoordinatorLayout>
